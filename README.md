# AI Service Deployment

This project demonstrates how to deploy an AI model (FLAN-T5) as an API using FastAPI and Docker.

## Features
- Hugging Face model integration
- FastAPI backend
- Docker containerization
- Deployment on GCP (Compute Engine)

## Usage
Run locally:
uvicorn main:app --reload
